# Git & GitHub 2일차 학습 정리

**날짜:** 2026-08-11 ~ 2026-08-12

---

## 1. 브랜치 병합 실습 (Fast-Forward Merge)

기준 브랜치(main)에 새 커밋이 없는 상태에서 브랜치를 합치면, 별도의 병합 커밋 없이
브랜치 포인터만 앞으로 이동한다.

```bash
git branch left
git branch right

git switch left
# left.py 생성
git add left.py
git commit -m "Create left.py"

git switch right
# right.py 생성
git add right.py
git commit -m "Create right.py"

git switch main
git merge right          # Fast-Forward 병합
```

## 2. 브랜치 병합 실습 (3-Way Merge)

main과 다른 브랜치가 **공통 조상 이후 각자 다른 커밋**을 쌓았을 때는
두 브랜치 + 공통 조상을 비교해 새로운 **병합 커밋(merge commit)**이 생긴다.

```bash
git branch a
git branch b

git switch a
# 파일 수정 후
git commit -am "Add a"

git switch b
# 파일 수정 후
git commit -am "Add b"

git switch main
git merge a
git merge b               # 3-Way Merge → 병합 커밋 생성
```

`git log --all --graph`로 확인하면 브랜치가 합쳐지는 지점이 그래프로 남는다.

## 3. Merge Conflict 발생과 해결

같은 파일(`hello.py`)의 같은 줄을 두 브랜치에서 서로 다르게 수정한 뒤 병합을 시도하면
충돌이 발생한다. Git이 자동으로 판단할 수 없어서 직접 결정해야 한다.

```python
<<<<<<< HEAD
print("hello, A")
=======
print("hello, B")
>>>>>>> b
```

충돌 마커를 정리해서 최종 내용을 `print("hello, A & B")`로 합치고,
다시 add/commit해서 병합을 완료했다.

```bash
git add hello.py
git commit -m "merge conflict in hello.py"
```

## 4. 원격 저장소 연결과 push

로컬 저장소에서 작업한 내용을 GitHub(원격 저장소)에 올리는 과정을 연습했다.

```bash
git remote add origin <원격 저장소 URL>
git remote -v                  # 등록된 원격 저장소 확인

git add hello.txt README.md
git commit -m "Create hello.txt"
git commit -m "Create README.md"

git push origin main
```

## 5. Pull Request 실습

기능 브랜치(`feat/pr`)를 만들어 작업한 뒤, main으로 바로 merge하지 않고
GitHub에서 **Pull Request**를 열어 병합하는 흐름을 연습했다.

```bash
git switch -c feat/pr
# pr.py 생성
git add pr.py
git commit -m "Create pr.py"

# 리뷰 중 발견한 오타 수정
git add pr.py
git commit -m "fix pr.py"

git push origin feat/pr
```

이후 GitHub 저장소에서 `feat/pr` → `main`으로 Pull Request(#1)를 생성하고,
**Merge pull request**로 병합을 완료했다.

## 6. 오늘 배운 핵심 명령어 정리

```bash
# 브랜치 & 병합
git branch <이름>
git switch <이름>
git switch -c <이름>       # 브랜치 생성 + 이동
git merge <브랜치명>
git log --all --graph

# 원격 연결 & 동기화
git remote add origin <URL>
git remote -v
git push origin <브랜치명>
git pull origin main
```

## 7. 느낀 점

- Fast-Forward와 3-Way Merge가 결과 화면(`git log --graph`)에서 어떻게 다르게 보이는지
  직접 충돌을 만들어보고 나서야 확실히 이해가 됐다.
- PR을 거치는 이유는 단순히 "합치는 절차"가 아니라, 병합 전에 변경사항을 리뷰하고
  (실제로 `pr.py`의 오타를 병합 전에 수정함) 문제를 걸러내는 단계라는 걸 실습으로 체감했다.
