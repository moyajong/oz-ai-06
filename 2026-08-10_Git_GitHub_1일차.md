# Git & GitHub 1일차 학습 정리

**날짜:** 2026-08-10

---

## 1. Git이 필요한 이유

- 코드는 계속 수정되는데, 예전 버전으로 되돌리거나 누가 언제 무엇을 바꿨는지 추적할 방법이 필요하다.
- 여러 사람이 동시에 같은 프로젝트를 작업할 때, 서로의 변경사항이 충돌 없이 합쳐질 방법이 필요하다.
- Git은 파일의 변경 이력을 스냅샷 단위로 기록하는 **버전 관리 시스템(VCS)** 이고,
  GitHub는 그 Git 저장소를 온라인에 올려 팀원들과 공유하는 **호스팅 플랫폼**이다.

## 2. 핵심 개념 3가지

| 개념 | 설명 |
|---|---|
| Repository (저장소) | 프로젝트의 변경 이력이 저장되는 공간. 로컬(내 컴퓨터)과 원격(GitHub) 두 곳에 존재할 수 있다 |
| Commit | 특정 시점의 변경사항을 하나의 스냅샷으로 저장하는 단위. 메시지로 "무엇을 왜 바꿨는지" 남긴다 |
| Branch | 메인 흐름(main)에서 갈라져 나온 독립적인 작업 라인. 실험적인 작업을 본체에 영향 없이 할 수 있다 |

## 3. 기본 작업 흐름

```bash
git init                    # 새 저장소 초기화
git status                  # 변경된 파일 확인
git add <파일명>             # 커밋할 대상으로 등록 (staging)
git commit -m "메시지"       # 스냅샷 기록
git log                     # 커밋 이력 확인
```

## 4. 오늘 실습한 내용

개인 실습 저장소(`oz-ai-06`)에서 아래 순서로 진행했다.

1. `.gitignore` 작성 → 버전 관리에서 제외할 파일 패턴 등록
2. `hello.py` 생성 및 첫 커밋 → `git add` → `git commit` 흐름 연습
3. `ai.py` 생성 및 커밋 → 커밋을 여러 번 반복하며 `git log`로 이력이 쌓이는 것을 확인

```bash
git add .gitignore
git commit -m "Create .gitignore"

git add hello.py
git commit -m "Create hello.py"

git add ai.py
git commit -m "Create ai.py"
```

## 5. 오늘 배운 것 요약

- `commit`은 저장이 아니라 "의미 있는 변경 단위"로 나누는 것이 중요하다는 걸 체감했다.
  (파일 하나 바꿀 때마다 커밋 메시지를 남기니 나중에 `git log`만 봐도 무슨 일이 있었는지 파악이 된다.)
- `.gitignore`를 미리 설정해두지 않으면 나중에 불필요한 파일(가상환경, 캐시 등)이 저장소에 섞여 들어갈 수 있다는 것을 알게 됐다.
- 브랜치와 원격 저장소는 아직 다루지 않았고, 2일차에 이어서 학습할 예정이다.
