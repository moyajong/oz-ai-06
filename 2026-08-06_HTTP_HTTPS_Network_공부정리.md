# 웹 서비스 기초와 구조 2일차 학습 정리

**날짜:** 2026-08-06

---

## 1. 네트워크 통신의 기본 계층

인터넷에서 데이터가 오가는 과정은 여러 계층으로 나눠져 있다 (TCP/IP 모델 기준).

| 계층 | 역할 | 대표 프로토콜 |
|---|---|---|
| 애플리케이션 계층 | 실제 서비스 데이터 형식 | HTTP, HTTPS, DNS |
| 전송 계층 | 데이터를 신뢰성 있게(또는 빠르게) 전달 | TCP, UDP |
| 인터넷 계층 | 목적지까지 경로를 찾음 | IP |
| 네트워크 접근 계층 | 실제 물리적 전송 | Ethernet, Wi-Fi |

브라우저에서 URL을 입력하면 이 계층을 위에서 아래로 내려가며 패킷으로 쪼개져 전송되고,
서버에 도착하면 다시 아래에서 위로 조립되어 애플리케이션(HTTP) 레벨까지 올라온다.

## 2. DNS: 도메인 이름이 IP로 바뀌는 과정

브라우저는 `example.com` 같은 도메인 이름을 직접 이해하지 못하고, 실제로는 **IP 주소**로 통신한다.

```
example.com 입력
   → 로컬 DNS 캐시 확인
   → (없으면) DNS 서버에 조회 요청
   → IP 주소(예: 93.184.216.34) 응답
   → 그 IP로 TCP 연결 시작
```

## 3. TCP 3-way Handshake

HTTP 통신을 시작하기 전에, TCP는 먼저 연결을 맺는 절차를 거친다.

```
클라이언트 → 서버: SYN (연결 요청)
서버 → 클라이언트: SYN-ACK (요청 수락 + 나도 연결하고 싶음)
클라이언트 → 서버: ACK (확인)
--- 이제 데이터 전송 가능 ---
```

이 과정을 거쳐야 "신뢰할 수 있는" 연결이 만들어지고, 그 위에서 HTTP 요청/응답이 오간다.

## 4. HTTP vs HTTPS

- **HTTP**: 평문으로 데이터를 주고받는다. 중간에서 누군가 가로채면 내용이 그대로 노출된다.
- **HTTPS**: HTTP + **TLS(SSL) 암호화**. 통신 내용이 암호화되어 중간에서 가로채도 해독할 수 없다.

### TLS Handshake 개요
```
클라이언트 → 서버: 지원 가능한 암호화 방식 목록 전달
서버 → 클라이언트: 인증서(Certificate) + 선택한 암호화 방식 전달
클라이언트: 인증서 검증(신뢰할 수 있는 CA가 발급했는지 확인)
클라이언트 ↔ 서버: 대칭키 교환 → 이후 통신은 이 키로 암호화
```

인증서는 CA(공인 인증기관)가 "이 도메인은 진짜 이 서버가 맞다"는 걸 보증해주는 역할을 한다.

## 5. 쿠키(Cookie)와 세션(Session)

HTTP는 기본적으로 **Stateless(상태 비저장)** 프로토콜이라, 요청 하나하나가 서로 독립적이다.
로그인 상태 같은 걸 유지하려면 별도의 장치가 필요하다.

- **쿠키**: 서버가 클라이언트(브라우저)에 저장하도록 내려주는 작은 데이터. 이후 요청마다 자동으로 함께 전송된다.
- **세션**: 서버 쪽에 사용자 상태를 저장하고, 클라이언트에는 그 세션을 식별할 수 있는 `session id`만 쿠키로 내려준다.
- 최근에는 **JWT(JSON Web Token)** 처럼, 서버가 상태를 따로 저장하지 않고 토큰 자체에 사용자 정보를 담아 검증하는 방식도 많이 쓴다.

## 6. 오늘 배운 것 요약

- "인터넷이 연결되어 있다"는 게 당연해 보였는데, DNS 조회 → TCP 연결 → (HTTPS라면) TLS 협상까지 거친 뒤에야
  실제 HTTP 요청이 오간다는 걸 알게 됐다. 페이지 로딩이 느릴 때 이 중 어디서 지연되는지 짐작할 단서가 생겼다.
- HTTPS가 단순히 "자물쇠 아이콘"이 아니라, 실제로 TLS Handshake를 통해 암호화 키를 교환하는 절차라는 걸 이해했다.
- 로그인 유지가 되는 이유가 쿠키/세션 덕분이라는 것, 그리고 HTTP 자체는 상태를 기억하지 않는다는 걸 명확히 정리했다.
